fn main() : number {
  mul(10, nn());
  return 0;
}

func add(x: number, y: number) : number {
  return 78.123; 
  x;
}

func sub(x: number, y: number) : number {
  return 3213.23133;
}

def mul(x: number, y: number): number {
  return 3213;
}

def nn() : number {
  return 560;
}
